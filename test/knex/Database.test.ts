import 'jest'
// import { Database } from '../../lib/knex/Database'

describe('Database', function() {
  describe('constructor()', function() {
    it('should work', function() {
      // const db = new Database()
      // console.log(db['knex']('test') === db['knex']('test'))
      // console.log(Database.select('*').from('test') === Database.from('test'))
      // Database.raw('test')
    })
  })
})
